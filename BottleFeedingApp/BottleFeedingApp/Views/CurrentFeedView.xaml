<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BottleFeedingApp.Views.CurrentFeedView"             
             Style="{StaticResource PageStyle}"
             Title="Snapshot for Today">

  <ContentPage.ToolbarItems>
    <ToolbarItem  Text="+" Command="{Binding AddNewFeed}"/>
  </ContentPage.ToolbarItems>
  
  <Grid Padding="5" RowSpacing="1" ColumnSpacing="1" BackgroundColor="Default">

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Button Text="Record Feed"
            Grid.Row="1"
            Command="{Binding AddNewFeed}" />

    <ListView x:Name="currentFeedList"
              Grid.Row="2"
              ItemsSource="{Binding CurrentFeedData}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell Height="40">
            <StackLayout Padding="5">
              <Label
                Text="{Binding Title}" />
              <Label
                Text="{Binding Data}" />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

  </Grid>
</ContentPage>